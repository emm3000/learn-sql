/**
 * Site-wide configuration.
 *
 * SITE_NAME is the public product name. It lives here, in ONE place, so the
 * layout, landing page, and SEO meta all stay in sync (PRD Q1, resolved).
 */
export const SITE_NAME = 'pgGym';
